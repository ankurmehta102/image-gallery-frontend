const SearchBar = () => {
  return (
    <input
      className="ml-1 mr-3 w-96 rounded border border-custom-slate bg-inherit p-2 text-sm outline-none placeholder:text-sm"
      placeholder="Search"
    />
  );
};

export default SearchBar;
